1. Display snum, sname, city and comm of all salespeople.
Q: SELECT snum, sname, city, comm FROM salespeople;

2. Display all snum without duplicates from all orders.
Q: SELECT DISTINCT snum FROM orders;

3. Display names and commissions of all salespeople in London.
Q: SELECT sname, comm FROM salespeople WHERE city = 'London';

4. All customers with rating of 100.
Q: SELECT * FROM cust WHERE rating = 100;

5. Produce orderno, amount and date from all rows in the order table.
Q: SELECT onum, amt, odate FROM orders;

6. All customers in San Jose, who have rating more than 200.
Q: SELECT * FROM cust WHERE city = 'San Jose' AND rating > 200;

7. All customers who were either located in San Jose or had a rating above 200.
Q: SELECT * FROM cust WHERE city = 'San Jose' OR rating > 200;

8. All orders for more than $1000.
Q: SELECT * FROM orders WHERE amt > 1000;

9. Names and cities of all salespeople in London with commission above 0.10.
Q: SELECT sname, city FROM salespeople WHERE city = 'London' AND comm > 0.10;

10. All customers excluding those with rating <= 100 unless they are located in Rome.
Q:  SELECT * FROM cust WHERE rating > 100 OR city = 'Rome';

11. All salespeople either in Barcelona or in London.
Q:  SELECT * FROM salespeople WHERE city IN ('Barcelona', 'London');

12. All salespeople with commission between 0.10 and 0.12 (exclusive).
Q:  SELECT * FROM salespeople WHERE comm > 0.10 AND comm < 0.12;

13. All customers with NULL values in city column.
Q:  SELECT * FROM cust WHERE city IS NULL;

14. All orders taken on Oct 3rd and Oct 4th 1994.
Q:  SELECT * FROM orders WHERE odate IN ('03-OCT-94', '04-OCT-94');

15. All customers serviced by Peel or Motika.
Q:  SELECT * FROM cust WHERE snum IN (SELECT snum FROM salespeople WHERE sname IN ('Peel', 'Motika'));

16. All customers whose names begin with a letter from A to B.
Q:  SELECT * FROM cust WHERE cname LIKE 'A%' OR cname LIKE 'B%';

17. All orders except those with 0 or NULL value in amt field.
Q:  SELECT * FROM orders WHERE amt IS NOT NULL AND amt <> 0;

18. Count the number of salespeople currently listing orders in the order table.
Q:  SELECT COUNT(DISTINCT snum) FROM orders;

19. Largest order taken by each salesperson, datewise.
Q:  SELECT snum, odate, MAX(amt) FROM orders GROUP BY snum, odate;

20. Largest order taken by each salesperson with order value more than $3000.
Q:  SELECT snum, MAX(amt) FROM orders WHERE amt > 3000 GROUP BY snum;

21. Which day had the highest total amount ordered.
Q:  SELECT odate, SUM(amt) AS total FROM orders GROUP BY odate ORDER BY total DESC LIMIT 1;

22. Count all orders for Oct 3rd.
Q:  SELECT COUNT(*) FROM orders WHERE odate = '03-OCT-94';

23. Count the number of different non-NULL city values in customers table.
Q:  SELECT COUNT(DISTINCT city) FROM cust WHERE city IS NOT NULL;

24. Select each customer’s smallest order.
Q:  SELECT cnum, MIN(amt) FROM orders GROUP BY cnum;

25. First customer in alphabetical order whose name begins with G.
Q:  SELECT cname FROM cust WHERE cname LIKE 'G%' ORDER BY cname LIMIT 1;

26. Get the output like “For dd/mm/yy there are ___ orders.”
Q:  SELECT odate, COUNT(*) FROM orders GROUP BY odate;

27. Assume that each salesperson has a 12% commission.
Q:  SELECT onum, snum, amt * 0.12 AS commission FROM orders;

28. Find highest rating in each city.
Q:  SELECT city, MAX(rating) FROM cust GROUP BY city;

29. Display totals of orders for each day in descending order.
Q:  SELECT odate, SUM(amt) AS total FROM orders GROUP BY odate ORDER BY total DESC;

30. All combinations of salespeople and customers who shared a city.
Q:  SELECT s.sname, c.cname, s.city FROM salespeople s, cust c WHERE s.city = c.city;

31. Name of all customers matched with the salespeople serving them.
Q:  SELECT c.cname, s.sname FROM cust c JOIN salespeople s ON c.snum = s.snum;

32. List each order number followed by the name of the customer.
Q:  SELECT o.onum, c.cname FROM orders o JOIN cust c ON o.cnum = c.cnum;

33. Names of salesperson and customer for each order after order number.
Q:  SELECT o.onum, s.sname, c.cname FROM orders o JOIN salespeople s ON o.snum = s.snum JOIN cust c ON o.cnum = c.cnum;

34. All customers serviced by salespeople with commission above 12%.
Q:  SELECT * FROM cust WHERE snum IN (SELECT snum FROM salespeople WHERE comm > 0.12);

35. Calculate commission on each order with rating above 100.
Q:  SELECT o.onum, o.amt, s.comm, o.amt * s.comm FROM orders o JOIN cust c ON o.cnum = c.cnum JOIN salespeople s ON o.snum = s.snum 
    WHERE c.rating > 100;

36. Find all pairs of customers having the same rating.
Q:  SELECT c1.cname, c2.cname, c1.rating FROM cust c1, cust c2 WHERE c1.rating = c2.rating AND c1.cnum <> c2.cnum;

37. Same as above but without duplicates.
Q:  SELECT c1.cname, c2.cname, c1.rating FROM cust c1, cust c2 WHERE c1.rating = c2.rating AND c1.cnum < c2.cnum;

38. Assign three salespeople to each customer (all combinations).
Q:  SELECT c.cname, s.sname FROM cust c, salespeople s;

39. Customers located in cities where Serres has customers.
Q:  SELECT * FROM cust WHERE city IN (SELECT city FROM cust WHERE snum = (SELECT snum FROM salespeople WHERE sname = 'Serres'));

40. Pairs of customers served by single salesperson.
Q:  SELECT c1.cname, c2.cname, c1.snum FROM cust c1, cust c2 WHERE c1.snum = c2.snum AND c1.cnum < c2.cnum;

41. Pairs of salespeople living in same city.
Q:  SELECT s1.sname, s2.sname, s1.city FROM salespeople s1, salespeople s2 WHERE s1.city = s2.city AND s1.snum < s2.snum;

42. Pairs of orders by same customer.
Q:  SELECT o1.cnum, o1.onum, o2.onum FROM orders o1, orders o2 WHERE o1.cnum = o2.cnum AND o1.onum < o2.onum;

43. Customers with same rating as Hoffman.
Q:  SELECT cname, city FROM cust WHERE rating = (SELECT rating FROM cust WHERE cname = 'Hoffman');

44. Extract all orders of Motika.
Q:  SELECT * FROM orders WHERE snum = (SELECT snum FROM salespeople WHERE sname = 'Motika');

45. Orders credited to salesperson serving Hoffman.
Q:  SELECT * FROM orders WHERE snum = (SELECT snum FROM cust WHERE cname = 'Hoffman');

46. Orders greater than average for Oct 4.
Q:  SELECT * FROM orders WHERE amt > (SELECT AVG(amt) FROM orders WHERE odate = '04-OCT-94');

47. Average commission of salespeople in London.
Q:  SELECT AVG(comm) FROM salespeople WHERE city = 'London';

48. Orders attributed to salespeople servicing customers in London.
Q:  SELECT * FROM orders WHERE snum IN (SELECT snum FROM cust WHERE city = 'London');

49. Commissions of salespeople servicing customers in London.
Q:  SELECT DISTINCT comm FROM salespeople WHERE snum IN (SELECT snum FROM cust WHERE city = 'London');

50. Customers whose cnum is 1000 above the snum of Serres.
Q:  SELECT * FROM cust WHERE cnum = (SELECT snum + 1000 FROM salespeople WHERE sname = 'Serres');

51. Count the customers with rating above San Jose’s average.
Q:  SELECT COUNT(*) FROM cust WHERE rating > (SELECT AVG(rating) FROM cust WHERE city = 'San Jose');

52. Obtain all orders for the customer named Cisnerous.
Q:  SELECT *FROM orders WHERE cnum = (SELECT cnum FROM cust WHERE cname = 'Cisnerous');

53. Names and rating of customers who have above average orders.
Q:  SELECT DISTINCT c.cname, c.rating FROM cust c JOIN orders o ON c.cnum = o.cnum WHERE o.amt > (SELECT AVG(amt) FROM orders);

54. Total amount in orders for each salesperson greater than largest order.
Q:  SELECT snum, SUM(amt) FROM orders GROUP BY snum HAVING SUM(amt) > (SELECT MAX(amt) FROM orders);

55. Find all customers with orders on 3rd Oct.
Q:  SELECT DISTINCT c.* FROM cust c JOIN orders o ON c.cnum = o.cnum WHERE o.odate = '03-OCT-94';

56. Names and numbers of salespeople who have more than one customer.
Q:  SELECT s.snum, s.sname FROM salespeople s JOIN cust c ON s.snum = c.snum GROUP BY s.snum, s.sname HAVING COUNT(c.cnum) > 1;

57. Check if correct salesperson was credited with each sale.
Q:  SELECT o.onum, o.snum, c.snum FROM orders o JOIN cust c ON o.cnum = c.cnum WHERE o.snum <> c.snum;

58. Orders with above average amounts for their customers.
Q:  SELECT * FROM orders o WHERE amt > (SELECT AVG(amt) FROM orders WHERE cnum = o.cnum);

59. Sum of amounts grouped by date where sum >= 2000 above max amount.
Q:  SELECT odate, SUM(amt)FROM orders GROUP BY odate HAVING SUM(amt) >= (SELECT MAX(amt) + 2000 FROM orders);

60. Customers with ratings equal to maximum for their city.
Q:  SELECT c1.cnum, c1.cname, c1.city FROM cust c1 WHERE rating = (SELECT MAX(rating) FROM cust c2 WHERE c2.city = c1.city);

61. Salespeople who have customers in their cities who they don’t service.

-- Using JOIN
SELECT DISTINCT s.sname 
FROM salespeople s 
JOIN cust c1 ON s.city = c1.city 
WHERE c1.snum <> s.snum;
-- Using correlated subquery
Q:  SELECT sname FROM salespeople s WHERE EXISTS(SELECT * FROM cust c WHERE c.city = s.city AND c.snum <> s.snum);

62. Extract customers if at least one customer is in San Jose.
Q:  SELECT cnum, cname, city FROM cust WHERE EXISTS (SELECT * FROM cust WHERE city = 'San Jose');

63. Salespeople who have multiple customers.
Q:  SELECT snum FROM cust GROUP BY snum HAVING COUNT(*) > 1;

64. Salespeople number, name and city who have multiple customers.
Q:  SELECT s.snum, s.sname, s.city FROM salespeople s WHERE s.snum IN (SELECT snum FROM cust GROUP BY snum HAVING COUNT(*) > 1);

65. Salespeople who serve only one customer.
Q:  SELECT snum FROM cust GROUP BY snum HAVING COUNT(*) = 1;

66. Salespeople with more than one current order.
Q:  SELECT s.*FROM salespeople s WHERE s.snum IN (SELECT snum FROM orders GROUP BY snum HAVING COUNT(*) > 1);

67. Salespeople who have customers with rating 300 (EXISTS).
Q:  SELECT * FROM salespeople s WHERE EXISTS (SELECT * FROM cust c WHERE c.snum = s.snum AND c.rating = 300);

68. Same using JOIN.
Q:  SELECT DISTINCT s.* FROM salespeople s JOIN cust c ON s.snum = c.snum WHERE c.rating = 300;

69. Salespeople with customers in their city not assigned to them (EXISTS).
Q:  SELECT * FROM salespeople s WHERE EXISTS (SELECT * FROM cust c WHERE c.city = s.city AND c.snum <> s.snum);

70. Customers whose salesperson has at least one other customer with orders.
Q:  SELECT * FROM cust c WHERE EXISTS (SELECT * FROM cust c2 WHERE c2.snum = c.snum AND c2.cnum <> c.cnum AND c2.cnum IN (SELECT cnum FROM orders));

71. Salespeople with customers in their cities (ANY & IN).

-- IN
SELECT * FROM salespeople 
WHERE snum IN (
    SELECT snum FROM cust WHERE city = salespeople.city
);

-- ANY
Q:  SELECT * FROM salespeople s WHERE s.city = ANY (SELECT city FROM cust WHERE snum = s.snum);

72. Salespeople for whom customers follow them alphabetically.

-- EXISTS
SELECT * FROM salespeople s 
WHERE EXISTS (
    SELECT * FROM cust c WHERE c.cname > s.sname
);

-- ANY
Q:  SELECT * FROM salespeople WHERE sname < ANY (SELECT cname FROM cust);

73. Customers with rating greater than any customer in Rome.
Q:  SELECT * FROM cust WHERE rating > ANY (SELECT rating FROM cust WHERE city = 'Rome');

74. Orders greater than at least one order from Oct 6.
Q:  SELECT * FROM orders WHERE amt > ANY (SELECT amt FROM orders WHERE odate = '06-OCT-94');

75. Orders smaller than any amount for San Jose customers.
-- Using ANY
SELECT * FROM orders 
WHERE amt < ANY (
    SELECT amt FROM orders o 
    JOIN cust c ON o.cnum = c.cnum 
    WHERE c.city = 'San Jose'
);

-- Without ANY
Q:  SELECT * FROM orders WHERE amt < (SELECT MAX(o.amt) FROM orders o JOIN cust c ON o.cnum = c.cnum WHERE c.city = 'San Jose');

76. Customers with ratings higher than every customer in Paris.
-- Using ALL
SELECT * FROM cust 
WHERE rating > ALL (
    SELECT rating FROM cust WHERE city = 'Paris'
);

-- Using NOT EXISTS
Q:  SELECT * FROM cust c WHERE NOT EXISTS (SELECT * FROM cust WHERE city = 'Paris' AND rating >= c.rating);

77. Customers whose ratings >= ANY of Serres.
Q:  SELECT * FROM cust WHERE rating >= ANY (SELECT rating FROM cust WHERE snum = (SELECT snum FROM salespeople WHERE sname = 'Serres'));

78. Salespeople with no customers in their city.
Q:  SELECT * FROM salespeople s WHERE city <> ALL (SELECT city FROM cust WHERE snum = s.snum);

79. Orders greater than any for customers in London.
Q:  SELECT * FROM orders WHERE amt > ANY (SELECT amt FROM orders o JOIN cust c ON o.cnum = c.cnum WHERE c.city = 'London');

80. Salespeople and customers located in London.
Q:  SELECT sname, city FROM salespeople WHERE city = 'London'UNION SELECT cname, city FROM cust WHERE city = 'London';

81. Dates on which highest and lowest orders were taken per salesperson.
Q:  SELECT snum, MAX(odate), MIN(odate) FROM orders GROUP BY snum;

82. List all salespeople and indicate those with/without customers in their cities.
Q:  SELECT s.sname, CASE WHEN EXISTS (SELECT * FROM cust c WHERE c.city = s.city) THEN 'Has Customers' ELSE 'No Customers' END FROM salespeople s;

83. Append strings indicating if salesperson matched with customer in city.
Q:  SELECT s.sname || ' - ' || CASE WHEN EXISTS (SELECT * FROM cust c WHERE c.city = s.city AND c.snum = s.snum)THEN 'Matched' 
    ELSE 'Not Matched' END FROM salespeople s;

84. Union showing High/Low rating customers.
Q:  SELECT cname, city, rating, 'High Rating' FROM cust WHERE rating >= 200 UNION SELECT cname, city, rating, 'Low Rating' 
    FROM cust WHERE rating < 200;

85. Names and numbers of salespeople and customers with more than one order.
Q:  SELECT s.sname, s.snum FROM salespeople s WHERE s.snum IN (SELECT snum FROM orders GROUP BY snum HAVING COUNT(*) > 1)UNION
    SELECT c.cname, c.cnum FROM cust c WHERE c.cnum IN (SELECT cnum FROM orders GROUP BY cnum HAVING COUNT(*) > 1)ORDER BY 1;

86. Union of three queries.
Q:  SELECT snum FROM salespeople WHERE city = 'San Jose'UNION SELECT cnum FROM cust WHERE city = 'San Jose' UNION ALL
    SELECT onum FROM orders WHERE odate = '03-OCT-94';

87. Salespeople in London with at least one customer there.
Q:  SELECT * FROM salespeople s WHERE city = 'London' AND EXISTS (SELECT * FROM cust c WHERE c.city = 'London' AND c.snum = s.snum);

88. Salespeople in London without customers there.
Q:  SELECT * FROM salespeople s WHERE city = 'London' AND NOT EXISTS (SELECT * FROM cust c WHERE c.city = 'London' AND c.snum = s.snum);

89. Salespeople matched with customers including those without customers.
Q:  SELECT s.sname, c.cname FROM salespeople s LEFT JOIN cust c ON s.snum = c.snum UNION SELECT s.sname, c.cname FROM salespeople s, cust c 
    WHERE s.snum = c.snum;